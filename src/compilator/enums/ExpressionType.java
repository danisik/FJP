package compilator.enums;

public enum ExpressionType {
        IDENTIFIER,
        VALUE,
        MULTIPLICATION,
        ADDITIVE,
        RELATIONAL,
        LOGICAL,
        NEGATION,
        PAR
}
